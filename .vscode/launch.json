{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Deploy",
      "type": "PowerShell",
      "request": "launch",
      "script": "${workspaceFolder}\\deploy-staging.bat",
      "cwd": "${workspaceFolder}",
      "createTemporaryIntegratedConsole": true
    }
  ]
}
